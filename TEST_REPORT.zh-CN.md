# 发布测试报告

[English](./TEST_REPORT.md)

本页面是为下一次发布准备的静态验证快照，记录已经验证的范围、主动排除的操作，以及
仍需人工验证的项目。

## 验证概览

| 项目 | 结果 |
| --- | --- |
| 测试日期 | 2026-08-21 |
| 包版本 | `0.1.4`（尚未发布） |
| 自动测试 | **通过** — 20 个测试套件、456 项测试 |
| TypeScript 构建 | **通过** |
| npm 打包烟测 | **通过** — 88 个文件；压缩后 172.9 kB；解压后 676.1 kB |
| 真实 Garmin 集成 | **通过** — `global` 区域 8/8 项只读检查 |
| 中国区浏览器 MFA／DI 探针 | **通过** — 浏览器 MFA、一次性 ticket 交换与 profile 探测；未保存 session |

## 自动验证

在 Node.js 20 或更高版本的干净源码目录中，可以用以下命令复现发布检查：

```bash
npm ci
npm run test:coverage
npm run build
npm run pack:smoke
```

### 测试与覆盖率

| 指标 | 结果 |
| --- | ---: |
| 测试套件 | 20 个通过 |
| 测试 | 456 项通过 |
| 语句覆盖率 | 85.21% |
| 分支覆盖率 | 78.97% |
| 函数覆盖率 | 83.99% |
| 行覆盖率 | 87.46% |

`npm run build` 已成功完成。`npm run pack:smoke` 也已通过，检查的 npm 包包含
88 个文件（包含中英文测试报告页面），压缩后大小为 172.9 kB，解压后大小为
676.1 kB。

## 真实 Garmin 只读集成

本次集成验证使用本机私有 `.env`，连接 Garmin `global` 区域：

```bash
GARMIN_INTEGRATION_VERBOSE=false npm run test:integration
```

以下 8 项检查全部通过：

| 检查项 | 结果 |
| --- | --- |
| 身份验证／密码登录 | 通过 |
| 活动 | 通过 |
| 睡眠 | 通过 |
| 步数 | 通过 |
| 心率 | 通过 |
| 体重／身体成分 | 通过 |
| 训练库 | 通过 |
| 用户资料 | 通过 |

本次运行严格限于只读接口，没有创建、更新、安排或删除训练，也没有修改其他 Garmin
数据。运行时明确关闭了 verbose 输出，因此只显示状态和数量，不显示账号标识、活动
明细或健康数值。

## 中国区浏览器 MFA／DI 验证

经账号所有者明确同意，2026-08-21 使用隔离、可见的 Chrome 完成了真实中国区 MFA
流程。诊断程序在浏览器消费前截获一张短期 Garmin service ticket，仅向中国区 DI
端点交换一次，并成功探测中国区 profile API。全程只输出固定阶段名，没有打印邮箱、
密码、MFA 验证码、Cookie、ticket、Token、profile 数据或响应正文。

本次有意使用“不落盘”的 canary。它证明了真实的“浏览器 MFA → DI exchange → profile
probe”链路，但尚未证明保存 DI v2 session 后可以重启 dsh/MCP、轮换 refresh token，
并端到端调用普通 Garmin 工具。持久化与运行时行为已由离线自动测试完整覆盖，真实账号
验证仍作为单独发布门槛保留。

## FIT 导出验证

本次验证没有真实下载 FIT 文件，以免在未经用户明确同意时把个人活动文件写入本机。

自动测试已经覆盖 FIT 目标目录规则、ZIP 处理、CRC 校验和安全解压行为。新下载使用
以下目录结构：

```text
<GARMIN_FIT_DOWNLOAD_DIR>/
  GARMIN_FIT_<cn|global>_<规范化邮箱>/
    <activityId>.fit
```

已有的 `GARMIN_FIT_<邮箱>` 目录**不会自动迁移**。旧文件继续保留在原目录中；只有
新的下载会进入带区域标志的
`GARMIN_FIT_<cn|global>_<规范化邮箱>` 目录。

由于没有执行真实导出，本报告不声称已经端到端验证 Garmin 压缩包下载、本地 `.fit`
文件落盘，或将该文件导入设备及第三方应用。

## 已知验证缺口

以下场景尚未使用真实账号或客户端完成端到端验证：

- 保存真实中国区浏览器 session，并通过 dsh/MCP 使用及刷新它。
- Garmin 国际区（`global`）浏览器 MFA／DI 认证。
- WorkBuddy MCP 客户端烟测。
- ZCode MCP 客户端烟测。
- 真实 FIT 下载以及后续文件导入。

这些项目是本次报告明确记录的限制，不代表测试通过。

## 隐私与发布说明

- 集成测试使用的私有 `.env` 不属于本报告，禁止提交或发布。
- 本报告不包含密码、session token、MFA 验证码、账号标识、本地目标路径、活动明细
  或健康数值。
- 没有对 Garmin 数据执行写操作；浏览器 MFA canary 也没有持久化 session 文件。
- 当前包版本仍为 `0.1.4`；本验证快照及相关本地修改尚未发布。

下一次发布前应重新运行上述自动检查。真实 MFA、FIT 及客户端烟测只能在账号所有者
明确同意后执行，并继续使用同等的隐私保护措施。
